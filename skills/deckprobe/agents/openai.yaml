interface:
  display_name: "DeckProbe"
  short_description: "Inspect one local document and explain the result"
  default_prompt: "Use $deckprobe to inspect this local document; return a business-first check and the raw JSON report."
policy:
  allow_implicit_invocation: true
